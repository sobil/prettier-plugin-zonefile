{
  "name": "prettier-plugin-zonefile",
  "version": "1.0.0",
  "description": "A prettier DNS zonefile plugin",
  "main": "lib/index.js",
  "repository": "tbc",
  "author": "Simon Pilepich",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build --clean",
    "build:watch": "tsc --build --watch"
  },
  "devDependencies": {
    "@types/jest": "^26.0.13",
    "@types/prettier": "^2.1.0",
    "jest": "^26.4.2",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "prettier": "^2.1.1"
  }
}
